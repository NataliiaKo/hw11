Eloquent: Работа с связями
Создать таблицы:
    users
    posts
    categories
    tags (post_tags)
Создать модели для основных таблиц: User, Post, Category, Tag
Организовать связи внутри моделей:
        User:
                posts() // возвращает список постов пользователя
        Post:
                user() // возвращает автора поста
                category() // возвращает категорию поста
                tags() // возвращает теги поста
        Category:
                posts() // возвращает посты, которым присвоена категория
        Tag:
                posts() // возвращает посты, которым присвоен тег
//todo Для каждой из связей написать SQL запрос в формате комментария